#include<stdio.h>
int main()
{

    int a,b;
    int avg;

    // average of a and b

    printf("enter number a :");
    scanf("%d",&a);
    printf("enter number b :");
    scanf("%d",&b);

    avg = (a + b)/2;
   printf("average number a and b is :%d ;", avg);
    return 0;
}