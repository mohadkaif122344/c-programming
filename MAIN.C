#include<stdio.h>
int main()
{

    int a,b;
    int sum;

    // sum of two number
    printf("enter number a :");
    scanf("%d",&a);
    printf("enter number b :");
    scanf("%d",&b);

    sum = a + b;
    printf("the sum of a and b is : %d;", sum);

    return 0;
}